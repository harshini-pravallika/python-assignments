n=int(input('enter the number of elems you want: '))
l=list(map(int,input().split()))
ele=int(input('enter the ele to search: '))
check=0
for i in range(0,n):
     if l[i]==ele:
        print("the location is at",i)
        
     else:
        print("ele not found")
